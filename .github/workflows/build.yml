name: Build Unsigned IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Install CocoaPods
        run: |
          sudo gem install cocoapods
          pod install

      - name: Clean build folder
        run: rm -rf build

      - name: Archive app (UNSIGNED)
        run: |
          xcodebuild archive \
            -workspace Sample.xcworkspace \
            -scheme Sample \
            -configuration Release \
            -sdk iphoneos \
            -archivePath build/Sample.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_IDENTITY="" \
            PROVISIONING_PROFILE_SPECIFIER="" \
            DEVELOPMENT_TEAM="" \
            SKIP_INSTALL=NO

      - name: Inject embedded.mobileprovision (Xcode 15+ fix)
        run: |
          PROFILE=$(find /Applications/Xcode.app -name "*.mobileprovision" | head -n 1)
          echo "Using mobileprovision: $PROFILE"

          cp "$PROFILE" \
            build/Sample.xcarchive/Products/Applications/Sample.app/embedded.mobileprovision

      - name: Create unsigned IPA
        run: |
          cd build
          mkdir Payload
          cp -R Sample.xcarchive/Products/Applications/Sample.app Payload/
          zip -r Sample.ipa Payload

      - name: Verify embedded.mobileprovision exists
        run: |
          unzip -l build/Sample.ipa | grep embedded.mobileprovision

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: Sample-Unsigned-IPA
          path: build/Sample.ipa
